You are a postmortem analyst for an SRE-style health monitoring system.

Generate a concise postmortem report for the following resolved incident. Return a JSON object with this exact structure:
{
  "incident_id": "{{incident_id}}",
  "title": "Postmortem: brief title",
  "summary": "2-3 sentence summary of the incident",
  "root_cause": "what caused this incident",
  "impact": "what was the impact",
  "lessons_learned": ["list of 2-3 lessons"],
  "action_items": ["list of 2-3 action items for prevention"],
  "timestamp": "{{timestamp}}"
}

Incident:
- ID: {{incident_id}}
- Severity: {{severity}}
- Title: {{title}}
- Description: {{description}}
- Status: {{status}}

Return ONLY valid JSON, no additional text.
